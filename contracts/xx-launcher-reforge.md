# xx-launcher-reforge

Operator mill contract materialized from dispatch. Do not invent a different exam.

## Goal

REFORGE xx-launcher: two corrective generations failed and the old chain is closed. Author a FRESH root contract that answers EVERY ground from EVERY generation below — not just the newest finding; the one-finding-at-a-time loop is what died. Build on the newest surviving branch (laundry-bot/queue-xx-launcher-corrective) — do not rebuild what three builds already wrote and reviewed. Deliver to the same target as the root item. Grounds by generation:
  - xx-launcher: (kit: no FAIL)
  - xx-launcher-corrective: (kit: no FAIL)

### T1 — Implement the scoped goal

- files: app/src/main/AndroidManifest.xml
- verify: ./gradlew :app:testDebugUnitTest --offline

## Final gate

- verify: ./gradlew testDebugUnitTest --offline
