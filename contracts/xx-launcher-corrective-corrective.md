# xx-launcher-corrective-corrective

Operator mill contract materialized from dispatch. Do not invent a different exam.

## Goal

The defect review BLOCKED xx-launcher-corrective and its retry budget is spent. Reason: delivery refused: merge into main conflicts. Files:
app/src/main/AndroidManifest.xml

The build and the audit both passed; only the merge failed. The plan is already complete, so this is not a mill re Review head: # Review: xx-launcher-corrective - **Branch:** `laundry-bot/queue-xx-launcher-corrective` - **Laundry-bot exit code:** `0` - **Gate command:** `./gradlew testDebugUnitTest --offline` ## Commits ``` 8c64097 T1 — Implement the scoped goal [laundry-bot loop 2] ``` ## Files changed ``` app/src/main/AndroidManifest.xml | 7 +++++++ 1 file changed, 7 insertions(+) ``` ## Known-defect scan (exit 0) ``` [1mlaundry-bot/queue-xx-launcher-corrective[0m vs origin/main — 2 file(s) changed .gitignore app/src/main/AndroidManifest.xml [1mKnown-defect scan[0m clean — none of the six patterns that burned us before [1mExports with no test naming them[0m clean — every new export/tool is named by some test [1mSuites a gate should cover[0m (no python modules changed, or no test mentions them) ``` ## Known-defect scan (exit 0) ``` [1mlaundry-bot/queue-xx-launcher-corrective[0m vs origin/main — 2 file(s) changed .gitignore app/src/main/AndroidManifest.xml [1mKnown-defect scan[0m clean — none of the six patterns that burned us before [1mExports with no test naming them[0m clean — every new export/tool is named by some test [1mSuites a gate should cover[0m (no python modules changed, or no test mentions them) ``` ## Known-defect scan (exit 0) ``` [1mlaundry-bot/queue-xx-launcher-corrective[0m vs origin/main — 2 file(s) changed .gitignore app/src/main/AndroidManifest.xml [1mKnown-defect scan[0m clean — none of the six patterns that burned us before [1mExports with no test naming them[0m clean — every new export/tool is named by some test [1mSuites a gate should cover[0m (no python modules changed, or no test mentions them) ```

Scope a corrective contract that ANSWERS this finding on branch laundry-bot/queue-xx-launcher-corrective — fix the named defect, do not rebuild the item. Every task needs a behaviour-running verify that would have caught the finding. TASK SHAPE MATTERS: when the finding is a defect IN source code, the task declares that source file in files:. When the finding is a MISSING TEST for source that is already correct, the task's files: declares ONLY the test files — name the source in prose, never in files:, because a task that declares a source file it has no reason to change is voided by the exam-only rail every loop.

### T1 — Implement the scoped goal

- files: app/src/main/AndroidManifest.xml
- verify: ./gradlew :app:testDebugUnitTest --offline

## Final gate

- verify: ./gradlew testDebugUnitTest --offline
